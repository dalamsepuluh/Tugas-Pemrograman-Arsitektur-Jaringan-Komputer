#include <iostream>
#include <istream>
#include <fstream>
#include <stdlib.h>
using namespace std;

main() {
	cout << "*************** WELCOME TO LOGIN PAGE ***************" << endl;

} */
		void regist() {
			string regUser, regPass;
			system("cls")
				cout << "Enter your username: ";
				cin >> regUser;		
				cout << "Enter your password: ";
				cin >> regPass;

				//ofstream reg("databasePraktikum.txt", windows::app);
				reg << regUser << '|' << regPass << endl; //use differentiate to know which one is username and password 
				system("cls");
				cout << "Registration is successfull \n"
		}

		void login() {
			cout << "Please enter your username" << endl;
			cout << "Username: ";
			cin >> userNameAttempt;
			if (userNameAttempt == userName) {
				cout << "Password: ";
				cin >> passwordAttempt;
			}
